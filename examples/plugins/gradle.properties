# Plugin Configuration
# Customize these properties to configure your plugin

# Plugin metadata
pluginName=Maestro Example Plugins
pluginVersion=1.0.0
pluginDescription=Example wait and log plugins for Maestro automation framework
pluginJarName=maestro-example-plugins

# You can override these properties via command line:
# ./gradlew pluginJar -PpluginName="My Custom Plugin" -PpluginVersion="2.1.0"
