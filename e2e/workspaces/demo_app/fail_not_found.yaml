appId: com.example.example
tags:
    - failing
---
- launchApp:
    clearState: true
- evalScript: ${output.didFail = 1}
- runFlow:
    optional: true
    commands:
        - tapOn:
            id: non-existent-id
        - evalScript: ${output.didFail = 0}
- evalScript: ${output.didFail = 1}
